\documentclass[12pt,a4paper]{article}

% ── Font: professional LaTeX style (Latin Modern serif body, Latin Modern
% sans for headings, Inconsolata for monospace) ──────────────────────────────
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{inconsolata}
\usepackage{microtype}     % subtle kerning / line-break improvements

% ── Layout ───────────────────────────────────────────────────────────────────
\usepackage[margin=2.5cm, top=2.5cm, bottom=2.5cm]{geometry}
\usepackage{setspace}
\setstretch{1.15}
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}

% ── Float / page-break control ───────────────────────────────────────────────
\widowpenalty=10000
\clubpenalty=10000
\displaywidowpenalty=10000
% Discourage LaTeX from leaving big white space at end of page just to keep
% a float in [H] position. We use [!htbp] for floats below.
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.9}
\renewcommand{\textfraction}{0.07}
\renewcommand{\floatpagefraction}{0.85}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{3}
\setcounter{totalnumber}{5}

% ── Packages ─────────────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{placeins}     % \FloatBarrier to keep floats within sections
\usepackage[hidelinks, colorlinks=true, linkcolor=black, citecolor=blue!60!black, urlcolor=blue!60!black]{hyperref}

% ── Figures path ─────────────────────────────────────────────────────────────
\graphicspath{{../figures/}}

% ── Section formatting ──────────────────────────────────────────────────────
\usepackage{titlesec}
\titlespacing*{\section}{0pt}{14pt}{6pt}
\titlespacing*{\subsection}{0pt}{10pt}{3pt}
\titleformat{\section}{\Large\bfseries}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\large\bfseries}{\thesubsection}{0.5em}{}

% ── Captions: short, tight, below both figures and tables ───────────────────
\captionsetup{font=small, skip=4pt, labelfont=bf, justification=centering,
              singlelinecheck=true}
\captionsetup[table]{position=below, skip=4pt}
\captionsetup[figure]{position=below, skip=4pt}

% ── Lists ────────────────────────────────────────────────────────────────────
\usepackage{enumitem}
\setlist{itemsep=2pt, parsep=0pt, leftmargin=1.5em, topsep=4pt}

% ── Header/footer ────────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\fancypagestyle{plain}{\fancyhf{}\fancyfoot[C]{\thepage}\renewcommand{\headrulewidth}{0pt}}

% ══════════════════════════════════════════════════════════════════════════════
\begin{document}

% ── Title page ───────────────────────────────────────────────────────────────
\begin{titlepage}
    \centering
    \vspace*{3cm}

    {\Huge\bfseries Financial Tweet\\[8pt] Sentiment Classification\par}

    \vspace{1.5cm}

    {\Large A Systematic Comparison of Feature Extraction\\[4pt] and Classification Methods\par}

    \vspace{3cm}

    {\large
    \begin{tabular}{c}
        \textbf{Group 42}\\[12pt]
        Jan Luis Thier, 20250352\\
        Samuel Braun, 20250355\\
        Lukas Belser, 20250338\\
        Margarida Estrada Quintino, 20250411\\
    \end{tabular}
    \par}

    \vspace{2cm}

    {\large Text Mining -- 2025/2026\par}

    \vfill

    {\large NOVA Information Management School\par}
\end{titlepage}

% ── Table of Contents ────────────────────────────────────────────────────────
{\setlength{\parskip}{2pt}\tableofcontents}

\newpage

% ══════════════════════════════════════════════════════════════════════════════
\section{Introduction}
% ══════════════════════════════════════════════════════════════════════════════

Financial sentiment is what hedge funds, market-surveillance teams and retail investors all try to read out of social media in real time. Unlike general sentiment analysis, where ``great movie!'' is unambiguously positive, financial tweets mix specialised vocabulary (cashtags, jargon) with extreme brevity and a heavy class skew toward neutral commentary. A tweet stating ``stock down 5\% after earnings miss'' is bearish only to a reader who treats it as a directional claim rather than a factual report, and that ambiguity is exactly what makes the task hard.

We frame the problem as 3-class text classification: given a financial tweet, predict whether it expresses Bearish~(0), Bullish~(1) or Neutral~(2) sentiment. Our approach progresses from sparse bag-of-words representations through Word2Vec embeddings to frozen Transformer encoders, each combined with four standard classifiers, for a total of 32~feature\,$\times$\,classifier combinations evaluated under 5-fold stratified cross-validation. Each step is motivated by the limitations of the previous one.

Our frozen-encoder configuration (Twitter-RoBERTa embeddings combined with an XGBoost classifier) reaches a macro-F1 of 0.794, with the substantial margin over traditional approaches driven primarily by domain-aligned pretraining (Section~\ref{sec:results}). Fully fine-tuning the encoder lifts this to 0.840, and an equal-weight probability ensemble of fully fine-tuned Twitter-RoBERTa and FinBERT reaches \textbf{0.842} (Section~\ref{sec:finetune}). The ensemble is our submitted pipeline.

% ══════════════════════════════════════════════════════════════════════════════
\section{Data Exploration}
\label{sec:data}
% ══════════════════════════════════════════════════════════════════════════════

The dataset comprises 9,543~labeled training tweets and 2,388~unlabeled test tweets across three sentiment classes. It exhibits a strong class imbalance: Neutral tweets (64.7\%) outnumber Bearish tweets (15.1\%) by a factor of $4.3\times$ (Table~\ref{tab:class_dist}). Two consequences shape the rest of the pipeline: (i)~we use \texttt{class\_weight='balanced'} in every classifier, and (ii)~we adopt macro-F1 rather than accuracy as the primary metric, since a naive all-Neutral classifier would already reach 64.7\% accuracy while failing entirely on the minority classes.

\begin{table}[!htbp]
\centering
\begin{tabular}{lrr}
\toprule
\textbf{Class} & \textbf{Count} & \textbf{Proportion} \\
\midrule
Bearish (0) & 1,442 & 15.1\% \\
Bullish (1) & 1,923 & 20.2\% \\
Neutral (2) & 6,178 & 64.7\% \\
\bottomrule
\end{tabular}
\caption{Training-set class distribution.}
\label{tab:class_dist}
\end{table}

Tweet lengths range from 2 to 48~tokens (median:~18), and all three classes share similar length distributions (Figure~\ref{fig:tweet_length}), so length itself is not discriminative. This also justifies our \texttt{max\_length=128} setting for the Transformer tokenizers, since no tweet approaches that limit.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.78\textwidth]{tweet_length_distribution.png}
\caption{Tweet-length distribution across sentiment classes.}
\label{fig:tweet_length}
\end{figure}

Financial tweets carry distinctive surface markers: \$TICKER cashtags appear in ${\sim}$60\% of tweets, @mentions in ${\sim}$15\% and URLs in ${\sim}$20\%. Cashtags carry real contextual signal, but overfitting to specific tickers would not generalise, so we replace them with a \texttt{<TICKER>} placeholder.

Analysis of the most frequent tokens per class (Figure~\ref{fig:vocab}) reveals clear thematic separation: Bearish tweets feature words such as ``cut'', ``downgrade'' and ``sell''; Bullish tweets contain ``buy'', ``upgrade'' and ``breakout''; Neutral tweets use informational vocabulary (``earnings'', ``report'', ``market'') that overlaps with both sentiment classes, making the Neutral boundaries the primary source of classification error.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.78\textwidth]{top_tokens_per_class.png}
\caption{Top tokens per class after light cleaning.}
\label{fig:vocab}
\end{figure}

Data-quality checks found fewer than 1\% exact duplicates (retained, to avoid biasing the class proportions) and no label leakage: ``bearish'' and ``bullish'' appear naturally in financial discourse and represent legitimate patterns rather than annotation shortcuts.

% ══════════════════════════════════════════════════════════════════════════════
\section{Data Preprocessing}
\label{sec:preprocessing}
% ══════════════════════════════════════════════════════════════════════════════

\begin{sloppypar}
Preprocessing was implemented as a set of composable functions in \texttt{src/preprocessing.py} that we mix and match into named pipelines and benchmark under a fixed classifier.
\end{sloppypar}

\subsection{Cleaning Operations}

We split preprocessing into twelve standalone operations grouped by purpose (Table~\ref{tab:steps}). Surface cleanup (steps 1--4 and 7--10) standardises encoding, removes platform artefacts and folds case. Step 5 replaces \$TICKER cashtags with a placeholder so the encoder sees a consistent rare-token pattern rather than overfitting to specific stock names; it is included in every pipeline. Step 6 (number placeholders) and the two token-level operations (11--12) are reserved for the more aggressive variant, where their effect on downstream macro-F1 is measured directly. The stopword list is pruned to keep negations (\texttt{no}, \texttt{not}, \texttt{nor}) and directional words (\texttt{up}, \texttt{down}, \texttt{over}, \texttt{under}) because they flip sentiment polarity.

\begin{table}[!htbp]
\centering
\small
\begin{tabular}{cl}
\toprule
\textbf{\#} & \textbf{Operation} \\
\midrule
1  & HTML unescape (\texttt{\&amp;}, \texttt{\&lt;}) \\
2  & URL removal (\texttt{http://}, \texttt{www.}) \\
3  & @mention removal \\
4  & Hashtag stripping (keeps the word) \\
5  & Cashtag $\rightarrow$ \texttt{<TICKER>} \\
6  & Percentages $\rightarrow$ \texttt{<PCT>}; numbers $\rightarrow$ \texttt{<NUM>} \\
7  & Emoji $\rightarrow$ text description \\
8  & Unicode NFKC normalisation \\
9  & Lowercasing \\
10 & Repeated-punctuation collapse and whitespace normalisation \\
11 & Finance-aware stopword removal \\
12 & Lemmatisation or stemming \\
\bottomrule
\end{tabular}
\caption{The twelve cleaning operations used by our preprocessing pipelines.}
\label{tab:steps}
\end{table}

\subsection{Named Pipelines}

From these operations we built three named pipelines targeting different downstream models. \texttt{pp\_minimal} applies the surface-level operations (1--5 and 7--10) and is used for the bag-of-words and Word2Vec experiments. \texttt{pp\_aggressive} adds number replacement, stopword removal and lemmatisation or stemming on top, and is kept for ablation. \texttt{pp\_transformer} drops the steps that would interfere with the pretrained tokenizer (number replacement, Unicode normalisation, lowercasing) and is used for the Transformer encoders (Table~\ref{tab:pipelines}).

\begin{table}[!htbp]
\centering
\begin{tabular}{llp{6cm}}
\toprule
\textbf{Pipeline} & \textbf{Steps} & \textbf{Use case} \\
\midrule
\texttt{pp\_minimal}     & 1--5, 7--10 & BoW, TF-IDF, Word2Vec \\
\texttt{pp\_aggressive}  & 1--12       & Ablation only \\
\texttt{pp\_transformer} & 1--5, 7, 10 & Transformer encoders \\
\bottomrule
\end{tabular}
\caption{The three named pipelines and their target use cases.}
\label{tab:pipelines}
\end{table}

\subsection{Pipeline Comparison}

To isolate the preprocessing effect we benchmarked the three pipelines under a single fixed classifier (Logistic Regression on TF-IDF features, Table~\ref{tab:pp_compare}). The three variants land within a single percentage point of each other on the 80/20 stratified validation split, so preprocessing choice is a second-order knob on this task: our finance-aware stopword list already preserves polarity-flipping tokens, leaving little for the aggressive pipeline to gain or lose. We keep \texttt{pp\_minimal} as the bag-of-words/Word2Vec default because (i)~its tokens remain human-readable for the top-feature analysis in Section~\ref{sec:features}, and (ii)~stemming collapses ``falling''~/~``fell'' into the same form, which masks the temporal nuance the downstream model would otherwise pick up.

\begin{table}[!htbp]
\centering
\begin{tabular}{lr}
\toprule
\textbf{Pipeline} & \textbf{Macro-F1} \\
\midrule
\texttt{pp\_minimal}              & 0.748 \\
\texttt{pp\_aggressive} (lemma)   & 0.738 \\
\texttt{pp\_aggressive} (stem)    & \textbf{0.750} \\
\bottomrule
\end{tabular}
\caption{Preprocessing-pipeline comparison (LogReg\,+\,TF-IDF baseline).}
\label{tab:pp_compare}
\end{table}

% ══════════════════════════════════════════════════════════════════════════════
\section{Feature Engineering}
\label{sec:features}
% ══════════════════════════════════════════════════════════════════════════════

We implemented and compared eight feature-extraction methods across three families: sparse bag-of-words representations, dense Word2Vec embeddings and frozen Transformer encoders.

\subsection{Bag-of-Words Features}

The first family is the classical sparse representation. The bag-of-words variant uses scikit-learn's \texttt{CountVectorizer} with a 10\,000-feature unigram vocabulary. TF-IDF extends this to bigrams (\texttt{ngram\_range=(1,2)}) with sublinear term-frequency scaling, a vocabulary of up to 20\,000 features and the usual document-frequency cuts (\texttt{min\_df=2}, \texttt{max\_df=0.95}). TF-IDF outperforms BoW by one to three percentage points across every classifier in our comparison (Section~\ref{sec:results}), which confirms that bigrams recover some of the local word order that unigram counts throw away. The top weighted features per class (Figure~\ref{fig:tfidf}) are coherent with the discriminative vocabulary identified in Section~\ref{sec:data}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.78\textwidth]{top_tfidf_features.png}
\caption{Top TF-IDF features per class (Logistic Regression coefficients).}
\label{fig:tfidf}
\end{figure}

\subsection{Word Embeddings}

The second family is dense, mean-pooled token embeddings. We compared three variants. The first is a skip-gram Word2Vec model with 100~dimensions trained from scratch on the financial-tweet corpus. The second uses the pretrained GloVe-Twitter-100 vectors, which were trained on two billion tweets. The third is a TF-IDF--weighted pooling scheme that weights each token's embedding by its TF-IDF score before averaging, with the aim of down-weighting frequent function words.

All three variants underperform the bag-of-words family (macro-F1 0.59--0.66 against 0.72--0.74). The reason is mean pooling, which collapses word order: averaged GloVe vectors for ``stock is going up today'' and ``stock is going down today'' have a cosine similarity of 0.996, despite carrying opposite financial polarity. No downstream classifier can recover information that the pooling step has discarded.

\subsection{Transformer Encoders}

The third family is pretrained Transformer encoders used as frozen feature extractors. Each tweet is tokenised, passed through the encoder, and the last hidden states are mean-pooled (with attention masking applied to the padding positions) into a fixed 768-dimensional vector that is then handed to a scikit-learn classifier. Embeddings are cached on disk as \texttt{.npy} files, which makes the whole workflow CPU-feasible despite the size of the encoders.

The three encoders we use differ mainly in their pretraining domain (Table~\ref{tab:transformers}). DistilBERT is a general-purpose distilled BERT trained on Wikipedia and BookCorpus. FinBERT was pretrained on SEC filings and other financial communications. Twitter-RoBERTa was pretrained on 58~million tweets with sentiment labels, giving it both Twitter-domain coverage and sentiment-task awareness.

\begin{table}[!htbp]
\centering
\begin{tabular}{llrl}
\toprule
\textbf{Encoder} & \textbf{Pretraining domain} & \textbf{Params} & \textbf{Status} \\
\midrule
DistilBERT       & General-purpose text    & 66M  & Mandatory \\
FinBERT          & Finance (SEC filings)   & 110M & Extra work \\
Twitter-RoBERTa  & Twitter + sentiment     & 125M & Extra work \\
\bottomrule
\end{tabular}
\caption{The three Transformer encoders used as frozen feature extractors.}
\label{tab:transformers}
\end{table}

Domain alignment turns out to matter more than parameter count. DistilBERT, despite its 66~million parameters, underperforms TF-IDF bigrams. Twitter-RoBERTa, at roughly twice the size, achieves the best overall result. FinBERT lands between the two: it understands financial vocabulary, but its pretraining text is formal SEC filings rather than informal tweets.

A second observation concerns the tokenizer. All three encoders use subword vocabularies, so most financial vocabulary is fragmented at inference time. The word \texttt{bullish} becomes \texttt{bull|ish} and \texttt{downgrade} becomes \texttt{down|grade} for all three. \texttt{rebalancing} splits into four pieces for the BERT family but into three for RoBERTa. Our \texttt{<TICKER>} placeholder is itself split into several pieces, so the encoder never sees a single ``ticker'' token. What it does see is the same fragmentation pattern across every cashtag, which is the consistency the placeholder was meant to provide.

Finally, we deviate from the standard BERT-family practice in our choice of sentence representation: the canonical choice is the \texttt{[CLS]} token's hidden state, but we use a mean pool over all non-padding tokens. To check that this is not a silent performance loss we recomputed the embeddings with \texttt{[CLS]} pooling and ran the same downstream XGBoost classifier (Table~\ref{tab:pooling}). For the two sentiment-tuned encoders the two pooling choices are within noise, and mean pooling actually helps DistilBERT, whose \texttt{[CLS]} representation has not been adapted for sentiment.

\begin{table}[!htbp]
\centering
\begin{tabular}{lrrr}
\toprule
\textbf{Encoder} & \textbf{Mean pool} & \textbf{CLS pool} & \textbf{$\Delta$} \\
\midrule
Twitter-RoBERTa & .794$\pm$.006 & .792$\pm$.010 & $-$.002 \\
FinBERT         & .779$\pm$.003 & .776$\pm$.007 & $-$.003 \\
DistilBERT      & .674$\pm$.012 & .691$\pm$.010 & $+$.017 \\
\bottomrule
\end{tabular}
\caption{Mean vs.\ CLS pooling on the last hidden states (XGBoost, 5-fold CV).}
\label{tab:pooling}
\end{table}

% ══════════════════════════════════════════════════════════════════════════════
\section{Classification Models}
\label{sec:models}
% ══════════════════════════════════════════════════════════════════════════════

We evaluated four classifiers (Table~\ref{tab:classifiers}). All use class weighting to handle imbalance, 5-fold stratified cross-validation and \texttt{random\_state=42} for reproducibility.

\begin{table}[!htbp]
\centering
\small
\begin{tabularx}{\textwidth}{lXX}
\toprule
\textbf{Classifier} & \textbf{Key hyperparameters} & \textbf{Rationale} \\
\midrule
Logistic Regression & balanced, $C=1.0$, multinomial   & Interpretable linear baseline \\
LinearSVC           & balanced, $C=1.0$                 & Maximum-margin, sparse data \\
XGBoost             & 300 trees, depth 6, softprob       & Non-linear decision boundaries \\
Random Forest       & 300 trees, balanced                & Ensemble baseline \\
\bottomrule
\end{tabularx}
\caption{Classifiers and their key hyperparameters.}
\label{tab:classifiers}
\end{table}

\FloatBarrier

% ══════════════════════════════════════════════════════════════════════════════
\section{Results and Analysis}
\label{sec:results}
% ══════════════════════════════════════════════════════════════════════════════

\subsection{Full Results Matrix}

Table~\ref{tab:results} summarises macro-F1 across all 32~combinations: each row is one of the eight feature representations introduced in Section~\ref{sec:features}, each column is one of the four classifiers from Table~\ref{tab:classifiers}, and each cell is the mean and standard deviation across the five cross-validation folds. The best score per row is shown in bold and the overall best is underlined. Standard deviations stay below 0.017 throughout, and the winning configuration carries one of the lowest ($\pm$0.006), so the ranking is stable across folds rather than being an artefact of a particular split.

\begin{table}[!htbp]
\centering
\small
\begin{tabular}{l|cccc}
\toprule
\textbf{Features} & \textbf{LogReg} & \textbf{SVM} & \textbf{XGBoost} & \textbf{RandForest} \\
\midrule
BoW              & \textbf{.720$\pm$.011} & .708$\pm$.010 & .691$\pm$.013 & .664$\pm$.012 \\
TF-IDF           & .736$\pm$.006 & \textbf{.740$\pm$.013} & .670$\pm$.011 & .654$\pm$.014 \\
W2V-mean         & .600$\pm$.013 & .603$\pm$.012 & \textbf{.650$\pm$.006} & .595$\pm$.011 \\
W2V-tfidf        & .595$\pm$.013 & .610$\pm$.015 & \textbf{.658$\pm$.016} & .611$\pm$.011 \\
GloVe-mean       & .599$\pm$.017 & .617$\pm$.014 & \textbf{.636$\pm$.009} & .494$\pm$.004 \\
DistilBERT       & .702$\pm$.009 & \textbf{.705$\pm$.008} & .674$\pm$.012 & .517$\pm$.007 \\
FinBERT$^{*}$         & .736$\pm$.007 & .751$\pm$.009 & \textbf{.779$\pm$.003} & .734$\pm$.002 \\
Twitter-RoBERTa$^{*}$ & .755$\pm$.011 & .764$\pm$.008 & \underline{\textbf{.794$\pm$.006}} & .742$\pm$.008 \\
\bottomrule
\end{tabular}
\caption{Macro-F1 (mean\,$\pm$\,std) over 5-fold CV. Bold: best per row; underline: overall best. $^{*}$\,extra work.}
\label{tab:results}
\end{table}

\subsection{Key Findings}

Reading the matrix top to bottom by best score per row produces a clear feature hierarchy. Twitter-RoBERTa leads at 0.794 macro-F1, helped by sentiment-task pretraining on 58~million tweets. FinBERT comes second at 0.779: financial vocabulary helps, but the formal SEC-filings register is not an ideal match for tweets. TF-IDF bigrams reach 0.740 and stay competitive with the smaller Transformer, while plain bag-of-words trails another two points behind at 0.720. DistilBERT, the general-purpose encoder, lands at 0.705. That score sits below TF-IDF bigrams, which is the clearest evidence in the matrix that domain alignment matters more than encoder size. The Word2Vec variants sit at the bottom around 0.66, held there by the mean-pooling limitation discussed earlier.

The classifier dimension shows a complementary pattern. XGBoost dominates the dense Transformer rows, where non-linear decision boundaries in 768-d space pay off. Logistic regression and LinearSVC dominate the sparse rows, where their L2 regularisation handles the high dimensionality cleanly. Random Forest consistently lands last across both regimes.

\subsection{Per-Class Analysis}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.82\textwidth]{per_class_f1_top5.png}
\caption{Per-class F1 for the top-5 configurations.}
\label{fig:per_class}
\end{figure}

Neutral is consistently the easiest class (F1\,$>$\,0.87) thanks to a large support and distinctive factual vocabulary. Bearish and Bullish are harder (F1: 0.65--0.75) because they share domain vocabulary and differ primarily in polarity. Bearish is slightly harder still, since bearish tweets sometimes use hedging language (``might drop'') that resembles neutral commentary.

\subsection{Error Analysis}

The misclassified tweets cluster into three recurring patterns, all visible in the confusion matrices of Figure~\ref{fig:confusion}. The most common is the Neutral / Bearish boundary: tweets reporting negative news factually (``stock down 5\% after earnings miss'') are genuinely ambiguous between factual reporting and negative sentiment, and the dataset's annotations sometimes go one way and sometimes the other. The mirror-image Neutral / Bullish confusion arises from the same dynamic on positive news. Direct Bearish / Bullish swaps are rare and almost always involve sarcasm or contrarian positions, neither of which the encoder can resolve from text alone.

The dominant challenge across all three patterns is the fundamental ambiguity between factual reporting and sentiment expression in financial discourse. That ambiguity sits in the dataset's annotations rather than in the model.

\begin{figure}[H]
\centering
\begin{subfigure}[t]{0.46\textwidth}
    \includegraphics[width=\textwidth]{cm_twitter_roberta.png}
    \caption{Twitter-RoBERTa + XGBoost (best overall).}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.46\textwidth}
    \includegraphics[width=\textwidth]{cm_best_traditional.png}
    \caption{TF-IDF + SVM (best traditional).}
\end{subfigure}
\caption{Confusion matrices for the best Transformer and best traditional model.}
\label{fig:confusion}
\end{figure}

\subsection{Learning Curve and Statistical Significance}

The learning curve for the best configuration (Figure~\ref{fig:learning_curve}) plateaus after roughly 40\% of the training data, which suggests the model is near its ceiling for this feature representation. The persistent train--validation gap points to mild overfitting that more data alone will not resolve. Closing it would require architectural changes, in particular full fine-tuning of the encoder rather than a frozen-feature setup.

To confirm that the matrix ranking is statistically meaningful and not a fold-sampling artefact, we ran paired $t$-tests on the per-fold CV scores between the three best configurations. All three pairwise comparisons reach significance at $p < 0.05$, so the observed ranking is not produced by random fold variation.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.55\textwidth]{learning_curve.png}
\caption{Learning curve for Twitter-RoBERTa + XGBoost.}
\label{fig:learning_curve}
\end{figure}

\FloatBarrier
\clearpage
% ══════════════════════════════════════════════════════════════════════════════
\section{Extra Work}
\label{sec:extra}
% ══════════════════════════════════════════════════════════════════════════════

This section collects four contributions that go beyond the mandatory rubric, all marked EXTRA WORK in \texttt{tm\_tests\_42.ipynb}: two additional pretrained encoders, a zero-shot baseline, a LangChain-based classification agent and a decoder-LLM experiment.

\subsection{Domain-Specific Transformer Encoders}

\begin{sloppypar}
Beyond the mandatory DistilBERT, we evaluated two further pretrained encoders. FinBERT,\footnote{Hugging Face checkpoint \texttt{ProsusAI/finbert}.} pretrained on financial communications, reaches 0.779~macro-F1, a 7.4 percentage-point improvement over DistilBERT. Twitter-RoBERTa,\footnote{Hugging Face checkpoint \texttt{cardiffnlp/\allowbreak twitter-\allowbreak roberta-\allowbreak base-\allowbreak sentiment-\allowbreak latest}.} pretrained on 58~million tweets annotated with sentiment labels, reaches 0.794~macro-F1 and is the best configuration we tested. Both gains come almost entirely from the pretraining domain rather than from any architectural advantage over DistilBERT.
\end{sloppypar}

\subsection{Zero-Shot Pretrained Classification}
\label{sec:zero_shot}

The Twitter-RoBERTa checkpoint we use already ships with a 3-class sentiment head trained on tweets (negative\,/\,neutral\,/\,positive). This makes it possible to ask a complementary question: how much of the Twitter-RoBERTa + XGBoost result is contributed by the pretrained encoder, and how much by the downstream classifier we trained on top? To answer this we applied the pretrained head directly to the validation set, without any training of our own, and mapped its three outputs into our label space (negative\,$\rightarrow$\,Bearish, positive\,$\rightarrow$\,Bullish, neutral\,$\rightarrow$\,Neutral).

The zero-shot baseline reaches 0.587~macro-F1 (5-fold aggregation: $0.587 \pm 0.012$), well below the 0.794 obtained when the same encoder is paired with the trained XGBoost head (Table~\ref{tab:zero_shot}). The gap is concentrated on the minority classes: Bearish/Bullish F1 fall from the 0.65--0.75 range to 0.48--0.49, while Neutral degrades by considerably less. This is a label-space mismatch rather than a domain mismatch: a tweet using ``downgrade'', ``cut'' or ``miss'' is unambiguously bearish in finance, but not unambiguously negative in everyday language. The 0.21 gain that supervised adaptation recovers is the encoding of that financial label space, learned from $\sim$9.5k labeled tweets. Pretraining alignment is necessary but not sufficient.

\begin{table}[!htbp]
\centering
\begin{tabular}{lr}
\toprule
\textbf{Approach (Twitter-RoBERTa encoder)} & \textbf{Macro-F1} \\
\midrule
Zero-shot (pretrained 3-class head, no training)   & 0.587 \\
Frozen embeddings + XGBoost (ours)                 & \textbf{0.794} \\
\midrule
$\Delta$ from supervised adaptation                & \textbf{+0.207} \\
\bottomrule
\end{tabular}
\caption{Same encoder, two heads: zero-shot baseline vs.\ supervised adaptation.}
\label{tab:zero_shot}
\end{table}

\subsection{Agentic Workflow}

To demonstrate how the trained models can be composed into a higher-level decision-making system, we built a LangChain-based agent in \texttt{src/agent.py}. The agent exposes five tools to a language-model planner: \textit{classify\_tweet}, \textit{route\_model}, \textit{ensemble\_classify}, \textit{compare\_models} and \textit{explain\_\allowbreak prediction}. In normal operation it inspects the tweet's surface features to choose an appropriate classifier, reconciles disagreements between classifiers via majority vote, and produces interpretable explanations from the coefficients of the linear models. Three illustrative transcripts (full logs in \texttt{outputs/\allowbreak agent\_transcripts.md}) demonstrate each behaviour in turn.

In the routing scenario, the input ``\$AAPL all-time high, breakout above resistance.'' triggers the agent to detect a \$TICKER, route the request to FinBERT and return Bullish at 89.1\% confidence. In the ensemble scenario, the input ``Markets mixed; Fed signals rate changes amid inflation concerns.'' produces a split vote: TF-IDF/LogReg, FinBERT and Twitter-RoBERTa each return Neutral, while DistilBERT returns Bearish. The agent reconciles this to Neutral with 75\% agreement and flags the DistilBERT vote as an example of how a general-purpose encoder can over-weight non-financial sentiment cues such as ``concerns''. In the attribution scenario, the input ``Sell everything, this stock is going to crash hard.'' triggers the explanation tool, which extracts the largest LogReg coefficients: ``sell'' ($+2.85$) and ``crash'' ($+2.63$) are the strongest Bearish signals, and the same tokens carry negative Bullish weights of $-1.92$ and $-2.10$ respectively, actively pushing the prediction away from the positive class.

\subsection{Full Fine-tuning and Ensemble}
\label{sec:finetune}

The frozen-encoder + XGBoost configuration leaves the encoder--classifier boundary unoptimised. We progressively unfroze more of Twitter-RoBERTa and observed the lift on the validation split (Table~\ref{tab:finetune}). Unfreezing only the top two layers and the head (12\% of parameters trainable, 4 epochs, LR $2\mathrm{e}{-5}$) lifts macro-F1 from 0.794 to 0.806. Fully unfreezing the encoder (3 epochs, LR $1\mathrm{e}{-5}$, AdamW, warmup ratio 0.1, weight decay 0.01) lifts it further to 0.840, with consistent per-class gains: Bearish F1 0.80, Bullish 0.81, Neutral 0.91. Applying the same recipe to FinBERT yields 0.803, weaker than RoBERTa, but the two models disagree on 12.7\% of validation tweets, and a 50/50 average of their softmax probabilities reaches \textbf{0.842}. Both component models are retrained on the full 9\,543~labelled tweets to generate the submitted \texttt{pred\_42.csv} (Section~\ref{sec:final}).

\begin{table}[!htbp]
\centering
\begin{tabular}{lr}
\toprule
\textbf{Configuration} & \textbf{Macro-F1} \\
\midrule
Frozen Twitter-RoBERTa embeddings + XGBoost                 & 0.794 \\
Last-2-layer fine-tuned Twitter-RoBERTa + head              & 0.806 \\
Fully fine-tuned FinBERT                                    & 0.803 \\
Fully fine-tuned Twitter-RoBERTa                            & 0.840 \\
50/50 probability average (RoBERTa + FinBERT, ours)         & \textbf{0.842} \\
\bottomrule
\end{tabular}
\caption{Progression from the frozen-encoder baseline to the submitted ensemble on the validation split.}
\label{tab:finetune}
\end{table}

\subsection{Decoder Model (Few-Shot)}
\label{sec:decoder}

To complete the encoder--decoder picture, we evaluated three Flan-T5 checkpoints with in-context few-shot prompting, two examples per class drawn from the training split. Each tweet is classified by a single forward pass through the seq2seq model with greedy decoding (max\_new\_tokens$=$5). Batched MPS inference brings the runtime to roughly 2.5~min for the small checkpoint, 35~min for base and 33~min for large, so the original CPU-only feasibility concern no longer applies.

Scaling helps substantially (Table~\ref{tab:decoder}). Flan-T5-small barely exceeds chance, predicting Neutral almost exclusively and never producing a confident Bullish label. Flan-T5-base reaches 0.310 by spreading its predictions across all three classes. Flan-T5-large adds another 17~percentage points to land at 0.485 macro-F1 with a markedly different error profile: it identifies Neutral very well (F1 0.80) and handles Bullish moderately (F1 0.56), but collapses on Bearish (recall 0.06). The model has learnt to recognise positive and neutral financial language but has no notion of the bearish direction beyond what the two negative examples provided.

Even at this scale the decoder remains below the zero-shot Twitter-RoBERTa baseline of Section~\ref{sec:zero_shot}, and a long way below the simplest trained encoder configuration. The conclusion is consistent across the three checkpoints: generic instruction-tuned models lack the financial bearish/bullish distinction that defines our minority classes, and two-shot prompts are not enough to teach it.

\begin{table}[!htbp]
\centering
\begin{tabular}{lr}
\toprule
\textbf{Approach} & \textbf{Macro-F1} \\
\midrule
Flan-T5-small (few-shot, 77M~params)  & 0.244 \\
Flan-T5-base  (few-shot, 248M~params) & 0.310 \\
Flan-T5-large (few-shot, 780M~params) & 0.485 \\
\midrule
Zero-shot Twitter-RoBERTa (Section~\ref{sec:zero_shot}) & 0.587 \\
TF-IDF + SVM (best traditional)                         & 0.740 \\
Twitter-RoBERTa + XGBoost (ours, best overall)          & \textbf{0.794} \\
\bottomrule
\end{tabular}
\caption{Few-shot decoder performance against encoder-based methods. Larger checkpoints help substantially, but even Flan-T5-large remains below the simplest pretrained-encoder baseline.}
\label{tab:decoder}
\end{table}

% ══════════════════════════════════════════════════════════════════════════════
\section{Final Pipeline and Predictions}
\label{sec:final}
% ══════════════════════════════════════════════════════════════════════════════

The production pipeline (\texttt{tm\_final\_42.ipynb}) retrains the ensemble of Section~\ref{sec:finetune} on the full 9\,543~labelled tweets and generates predictions for all 2\,388~test tweets. The steps are: (i)~preprocess with \texttt{pp\_transformer}; (ii)~fully fine-tune Twitter-RoBERTa for 3 epochs (AdamW, $\mathrm{LR}{=}1\mathrm{e}{-5}$, batch~16) and save its softmax probabilities on the test set; (iii)~repeat with FinBERT; (iv)~average the two probability tensors and take the argmax~$\rightarrow$~\texttt{pred\_42.csv}.

\begin{table}[!htbp]
\centering
\begin{tabular}{lrrr}
\toprule
\textbf{Class} & \textbf{Pred.\ count} & \textbf{Pred.\ \%} & \textbf{Train \%} \\
\midrule
Bearish & 359   & 15.0\% & 15.1\% \\
Bullish & 491   & 20.6\% & 20.2\% \\
Neutral & 1\,538 & 64.4\% & 64.7\% \\
\bottomrule
\end{tabular}
\caption{Test-set prediction distribution vs.\ training proportions.}
\label{tab:final_pred}
\end{table}

The prediction distribution closely matches the training proportions across all three classes (within 0.6 percentage points), suggesting the ensemble is well-calibrated relative to the class priors observed in training.

% ══════════════════════════════════════════════════════════════════════════════
\section{Conclusion}
\label{sec:conclusion}
% ══════════════════════════════════════════════════════════════════════════════

Our evaluation of 32 feature-by-classifier combinations identifies Twitter-RoBERTa combined with XGBoost as the best frozen-encoder pipeline (0.794 macro-F1, 5-fold stratified CV). Fully fine-tuning Twitter-RoBERTa lifts this to 0.840 on the validation split, and a 50/50 probability ensemble with fully fine-tuned FinBERT reaches \textbf{0.842} (Section~\ref{sec:finetune}). The ensemble is what we submit. Three findings stand out.

First, the dominant factor in the matrix is the pretraining domain of the encoder, not the parameter count. DistilBERT (66M params) underperforms TF-IDF bigrams while Twitter-RoBERTa (125M params) substantially exceeds both. Pretraining alignment alone is not sufficient either: the same Twitter-RoBERTa applied zero-shot reaches only 0.587 macro-F1 (Section~\ref{sec:zero_shot}), and supervised fine-tuning is what closes the gap to 0.840.

Second, feature extraction matters more than classifier choice. The gap between feature families (0.60--0.79~macro-F1) is roughly four times the gap between classifiers within the same family (0.02--0.05). Picking the right representation has by far the larger impact on this task.

Third, preprocessing is a second-order knob on this task. Once the finance-aware stopword list preserves the polarity-flipping tokens, the three pipelines land within a single percentage point of each other; explicit class-imbalance handling matters considerably more. Remaining limitations are computational: MPS fine-tuning was tractable for two encoders but would not scale to larger decoder LLMs or DeBERTa-v3-large.

% ══════════════════════════════════════════════════════════════════════════════
% Appendix: supporting figures and tables. Roman page numbering so the
% appendix does not count toward the 15-page body limit. Figure and table
% counters reset and use A.x numbering. Included in the TOC.
% ══════════════════════════════════════════════════════════════════════════════
\clearpage
\pagenumbering{roman}
\setcounter{page}{1}

\renewcommand{\thefigure}{A.\arabic{figure}}
\renewcommand{\thetable}{A.\arabic{table}}
\setcounter{figure}{0}
\setcounter{table}{0}

\appendix
\addcontentsline{toc}{section}{Appendix}
\section*{Appendix}

\subsection*{A.1\quad Embedding geometry}
We projected the 768-dimensional Twitter-RoBERTa embeddings of the validation set to two dimensions via PCA (Figure~\ref{fig:pca_app}). PC1 alone explains 26.7\% of variance and roughly tracks the Bearish/Bullish polarity axis; Neutral occupies the centre of the cloud. The three classes form overlapping but visibly distinct regions, which is the geometry the trained classifier exploits.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.70\textwidth]{pca_twitter_roberta.png}
\caption{PCA projection of Twitter-RoBERTa embeddings on the validation set (768-d to 2-d).}
\label{fig:pca_app}
\end{figure}

\subsection*{A.2\quad Confusion matrices for the remaining Transformer encoders}
Figures~\ref{fig:cm_finbert_app} and~\ref{fig:cm_distilbert_app} complete the set of confusion matrices for the frozen-encoder configurations referenced in Section~6.4.

\begin{figure}[!htbp]
\centering
\begin{subfigure}[t]{0.46\textwidth}
    \includegraphics[width=\textwidth]{cm_finbert.png}
    \caption{FinBERT + XGBoost.}
    \label{fig:cm_finbert_app}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.46\textwidth}
    \includegraphics[width=\textwidth]{cm_distilbert.png}
    \caption{DistilBERT + SVM.}
    \label{fig:cm_distilbert_app}
\end{subfigure}
\caption{Confusion matrices for FinBERT and DistilBERT, complementing Figure~6 in the body.}
\label{fig:confusion_app}
\end{figure}

\subsection*{A.3\quad Fine-tuning hyperparameters}
Table~\ref{tab:hyperparams_app} consolidates the training hyperparameters used for the three fine-tuned configurations in Section~7.4.

\begin{table}[!htbp]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lccc}
\toprule
\textbf{Hyperparameter} & \textbf{Last-2-layer FT} & \textbf{Full FT RoBERTa} & \textbf{Full FT FinBERT} \\
\midrule
Trainable parameters       & 14.8M / 124.6M    & 124.6M (all)      & 109.5M (all) \\
Epochs                     & 4                 & 3                 & 3 \\
Batch size                 & 16                & 16                & 16 \\
Learning rate              & $2\!\times\!10^{-5}$ & $1\!\times\!10^{-5}$ & $1\!\times\!10^{-5}$ \\
Weight decay               & 0.01              & 0.01              & 0.01 \\
Warmup ratio               & 0.1               & 0.1               & 0.1 \\
Optimiser                  & AdamW             & AdamW             & AdamW \\
Maximum sequence length    & 128               & 128               & 128 \\
\bottomrule
\end{tabular}
\caption{Training hyperparameters for the fine-tuned configurations.}
\label{tab:hyperparams_app}
\end{table}

\subsection*{A.4\quad Per-fold cross-validation scores}
Table~\ref{tab:cv_app} reports the per-fold macro-F1 scores that produce the means and standard deviations in Table~3 of the body, for the three configurations whose pairwise differences are statistically significant (Section~6.5).

\begin{table}[!htbp]
\centering
\small
\begin{tabular}{lccccccc}
\toprule
\textbf{Configuration} & \textbf{F1} & \textbf{F2} & \textbf{F3} & \textbf{F4} & \textbf{F5} & \textbf{Mean} & \textbf{Std} \\
\midrule
Twitter-RoBERTa + XGBoost  & .788 & .793 & .800 & .795 & .793 & \textbf{.794} & .006 \\
FinBERT + XGBoost          & .777 & .779 & .783 & .777 & .779 & .779          & .003 \\
TF-IDF + SVM               & .728 & .735 & .755 & .740 & .742 & .740          & .013 \\
\bottomrule
\end{tabular}
\caption{Per-fold macro-F1 (rounded to three decimals) for the three top-ranked frozen-encoder configurations.}
\label{tab:cv_app}
\end{table}

\end{document}
